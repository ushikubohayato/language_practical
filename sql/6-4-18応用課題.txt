今回の要件は注文製品に削除の概念を追加するものです。
この要件を満たすために今回は以下のテーブルを追加し、
既存の注文テーブルと紐づけて使用できるようにします。

テーブル名:ステータステーブル(order_statuses)

テーブル構成:
 ステータスID(store_id)
 ステータス名(store_name)

っプログラムで処理しやすいようにテーブルを分けて注文テーブルに紐づけて使用する形にしました。

sqlの解説:
 ステータステーブルを作成しました。
　注文テーブルのカラムにステータスIDの項目を追加し外部キー制約を付けました。
